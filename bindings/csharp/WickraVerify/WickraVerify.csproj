<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>Wickra.Verify</RootNamespace>
    <AssemblyName>Wickra.Verify</AssemblyName>

    <PackageId>Wickra.Verify</PackageId>
    <Version>0.1.0</Version>
    <Authors>kingchenc</Authors>
    <Description>Verify any backtest for .NET over the Wickra C ABI: recompute a claimed backtest report with the deterministic Wickra engine and confirm or refute it field by field.</Description>
    <PackageLicenseExpression>MIT OR Apache-2.0</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/wickra-lib/wickra-verify</PackageProjectUrl>
    <RepositoryUrl>https://github.com/wickra-lib/wickra-verify</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>trading;backtest;verify;determinism;blake3;ffi;native</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <NoWarn>$(NoWarn);NU5128;CS1591</NoWarn>
  </PropertyGroup>

  <!--
    Supported native runtime identifiers. The release pipeline builds the C ABI
    per target triple and stages the libraries under
    WickraVerify/runtimes/<rid>/native/ before `dotnet pack`:
      win-x64   win-arm64   linux-x64   linux-arm64   osx-x64   osx-arm64
  -->
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
    <None Include="runtimes/**/native/*" Pack="true" PackagePath="runtimes/" Condition="Exists('runtimes')" />
  </ItemGroup>

</Project>
