# Link the import library configure.win builds -- it references
# wickra_verify_abi.dll, not the package's own DLL. The C ABI header is staged into
# src/ by configure.win, so the include path is the source directory itself.
PKG_CPPFLAGS = -I.
PKG_LIBS = libwickra_verify_abi.dll.a
