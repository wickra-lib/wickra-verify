# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_verify.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKVERIFY_INC)
PKG_LIBS = -L$(WKVERIFY_LIB) -l:wickra_verify.dll
